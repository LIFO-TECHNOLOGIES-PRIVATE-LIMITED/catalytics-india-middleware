# Core dependencies
python-dotenv==1.0.0
requests==2.31.0

# PostgreSQL support (for direct verification)
psycopg2-binary==2.9.9

# Optional - for better XML parsing
lxml==5.1.0

# Dashboard UI
Flask==3.0.0
Flask-CORS==4.0.0
